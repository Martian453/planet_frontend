# Environment setup

## Bing Maps

Create a local file named `.env.local` (not committed) in the project root:

```bash
NEXT_PUBLIC_BING_MAPS_KEY=YOUR_BING_MAPS_KEY_HERE
```

Then restart the dev server:

```bash
npm run dev
```

