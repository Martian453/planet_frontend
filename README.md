# Air + Water Dashboard (UI Improvements)

## Run locally (Windows / PowerShell)

```powershell
cd "D:\AQI+Water\dashboard-ui-improvements"
npm install
npm run dev
```

- App: `http://localhost:3000`

## What’s included

- **Dashboard UI**: `app/page.tsx`
- **Theme support** (light/dark/system): `app/layout.tsx` + `components/theme-provider.tsx`
- **Sidebar navigation**: `components/sidebar-navigation.tsx`
- **Cards**:
  - `components/air-quality-card.tsx`
  - `components/environmental-core.tsx`
  - `components/water-quality-card.tsx`

## Reference assets

The original static reference implementation is stored in `reference/` (`dashboard.html`, `style.css`, `script.js`) for comparison.

